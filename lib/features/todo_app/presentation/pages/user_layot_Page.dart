import 'package:flutter/material.dart';

import '../widgets/user_todo_layout_widget.dart';
class UserLayoutPage extends StatelessWidget {
  const UserLayoutPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const UserWidget();
  }
}
